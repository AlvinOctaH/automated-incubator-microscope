# Auto-generated analysis report (embedded plots make it ~9.6MB despite being short) —
# excluded from GitHub's language detection so it doesn't overwhelm the actual Python/Arduino code.
mechanical/focus_report.html linguist-generated=true
